// import suppliers_page from '../../../../src/pages/inventory/suppliers/suppliers.vue'
// import { config, shallowMount } from '@vue/test-utils'

// let wrapper = {}
describe('suppliers.vue', () => {
  // render form title with form_title
  it('form_title add supplier', () => {
    //
  })
  it('form_title edit supplier', () => {
    //
  })

  // render form_status with supplier_status
  it('form_status not rendered when add supplier', () => {
    //
  })
  it('form_status rendered when edit supplier', () => {
    //
  })

  // render form with store
  it('form supplier filled information from store', () => {
    //
  })

  // click submit success: emit event & hide form
  it('submit success: emit event & hide form', () => {
    //
  })

  // click submit fail: render errors
  it('errors rendered', () => {
    //
  })
  
  // click close form: hide form
  it('close form', () => {
    //
  })
})